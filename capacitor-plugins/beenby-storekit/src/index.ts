/**
 * JS entry for the local Capacitor plugin package.
 *
 * The app does NOT import this file — src/lib/storekit.ts registers the plugin
 * directly with registerPlugin("BeenbyStoreKit"). This file only exists so that
 * `npx cap sync ios` recognises the package as a Capacitor plugin.
 */
export {};
