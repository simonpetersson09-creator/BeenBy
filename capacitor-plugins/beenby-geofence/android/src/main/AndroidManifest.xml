<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Geofencing: foreground + background location (the latter is what lets
         an arrival be detected while BeenBy is closed). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Local "Är du hos …?" arrival notification (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android clears geofences on reboot; re-register them afterwards. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <receiver
            android:name="app.beenbys.geofence.GeofenceBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="app.beenbys.geofence.ArrivalActionReceiver"
            android:exported="false" />
        <receiver
            android:name="app.beenbys.geofence.GeofenceBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
