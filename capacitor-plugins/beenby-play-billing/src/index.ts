/**
 * JS entry for the local Capacitor plugin package.
 *
 * The app does NOT import this file — src/lib/playBilling.ts registers the
 * plugin directly with registerPlugin("BeenbyPlayBilling"). This file only
 * exists so that `npx cap sync android` recognises the package.
 */
export {};
